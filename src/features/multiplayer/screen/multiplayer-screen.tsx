/**
 * Multiplayer Screen
 *
 * Main screen component for multiplayer feature
 */

import { GameModeSelectionScreen } from './parts/game-mode-selection-screen';

export function MultiplayerScreen() {
  return <GameModeSelectionScreen />;
}
