/**
 * API Endpoints Configuration
 *
 * Centralized endpoint definitions
 * Example structure - customize based on your API
 */

// Placeholder endpoint helper
// Replace with your actual endpoint helper from @app/lib
// const createEndpoint = (path: string) => path;

export const Endpoints = {
  // Example endpoints - customize for your application
  // USER: '/api/v1/user',
  // AUTH: {
  //   LOGIN: '/api/v1/auth/login',
  //   LOGOUT: '/api/v1/auth/logout',
  // },
} as const;
